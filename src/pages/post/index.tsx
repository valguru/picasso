import React from "react";
import {PostDetails} from "../../widgets/postDetails/ui";

export const PostPage: React.FC = () => {
    return (
        <div className="container">
            <PostDetails />
        </div>
    )
}